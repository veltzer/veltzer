# Lets `uv sync` install this repo's Python dependencies.

[project]
name = "veltzer"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = [
    "PyYAML",
    "pylint",
    "ruff",
    "mypy",
]

[dependency-groups]
dev = [
    "mypy",
    "pylint",
    "pytest",
    "ruff",
    "types-PyYAML",
]

[tool.uv]
# Demo/teaching repo: not an installable package.
package = false
